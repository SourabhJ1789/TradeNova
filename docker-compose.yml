version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  frontend:
    image: node:18
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: ["npx", "vite", "preview", "--port", "5173", "--host"]
    ports:
      - "5173:5173"
